<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Workout</title>
  <link rel="stylesheet" href="/css/style.css"> 
</head>
<body>
  <%- include('partials/header') %>

<h2>Edit Workout</h2>

<form action="/workouts/edit/<%= workout._id %>" method="POST">
  <input type="hidden" name="method" value="PUT"> <!-- Specify the method as PUT -->
  
  <label for="name">Name</label>
  <input type="text" id="name" name="name" value="<%= workout.name %>" required>

  <br><label for="duration">Duration (minutes)</label>
  <input type="number" id="duration" name="duration" value="<%= workout.duration %>" required>

  <br><label for="date">Date</label>
  <input type="date" id="date" name="date" value="<%= workout.date.toISOString().split('T')[0] %>" required>

  <br><label for="description">Description</label>
  <textarea id="description" name="description"><%= workout.description %></textarea>

  <br><button type="submit" class="btn btn-primary">Update Workout</button>
</form>
  <%- include('partials/footer') %>
</body>
</html>